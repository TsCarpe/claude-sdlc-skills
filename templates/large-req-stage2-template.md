# 大需求第二段产物模板（design.md + implement.md 骨架）

> **用途**：大需求三段式规划第二段（技术骨架）的产物骨架。规划时分别复制到 parent `design.md`（下半部）与 `implement.md` 逐节填充。
> **规则源**：`docs/large-req-playbook.md`「第二段」——本文只是模板，条款冲突以 playbook 为准。
> **核心定位**：承上启下——把第一段业务全貌翻译成技术骨架，给 child prd 提供「不用现场设计」的技术依据。child prd 应只做内联+裁剪；若一半内容是现场设计，说明本段缺项。

---

# design.md 骨架（下半部）

> 输入源：parent `prd.md`（全貌文档）+ ER 基线；上半部为字段级 ER（第一段产物）

## 1. 数据模型落地

表结构现状（已建/待建）、DDL 约束要点（唯一约束/索引策略及理由）。

## 2. 接口契约（分级粒度，每行挂 F 编号 + 消费的业务规则/主线节点）

**写接口（字段级：字段名/嵌套结构/口径来源/校验归属；不含校验注解细节）**：

| F 点 | URL | 字段（嵌套逐级列） | 口径来源（业务规则/ER 字段） | 校验归属（声明式校验/校验器） |
|---|---|---|---|---|
| F03 | /xxx/save | … | 规则§x / ER.<主表>.* | 基础注解 + 校验器跨字段 |

**读接口（出参字段清单 + 口径来源；展示细节留 child）**：

| F 点 | URL | 出参字段清单 | 口径来源（挂 D 表决策） |
|---|---|---|---|
| F01 | /xxx/get_list | … | D5 统计口径 / D9 推导 |

## 3. 公共资产清单（六类；每项标：被哪些 child 消费 / 归属哪个 child 交付）

| 类别 | 资产 | 内容要点 | 消费 child | 交付 child |
|---|---|---|---|---|
| ① 枚举与码表 | XxxEnum（含跨域公共枚举扩展值→**声明影响面**） | code/label 对齐 ER 与实体注释 | … | 骨架 child |
| ② 常量与 key 空间 | 锁 key / 定时任务 taskId / 错误消息文案模板 | 统一收口，禁止 child 内散落定义 | … | 骨架 child |
| ③ 命名空间约定 | URL 前缀 / 包路径 / 类名前缀 / 校验 profile 前缀 / 方法名避让 | … | 全部 | 骨架 child |
| ④ 公共 DTO 子结构 | 文件信息体 / 列表行公共字段 / 快照结构 | 多 child 同构结构只定一版 | … | 骨架 child |
| ⑤ 共享存量扩展影响 | 改哪些公共 Req/组件 | 受影响 profile/域清单 | … | 对应 child |
| ⑥ DDL 与索引脚本 | 表清单 + uk/idx 策略 | 落库脚本路径 | … | 骨架 child |

> 边界：不重复项目通用规范约定（分页工具/通用字段名等），只收本需求域新增/扩展项。

## 4. 分层文件规划（各层新增文件清单）

| 层 | 新增 |
|---|---|
| <按项目分层列出，如 api / controller / service / dao …> | … |

## 5. 公共组件清单（复用/新建分列）

| 组件 | 职责 | 依据功能点（防过度抽象） | 遵循规范（项目规范文档路径） |
|---|---|---|---|
| 复用：… / 新建：… | … | … | … |

## 6. 关键实现决策 D 表（必答类别，漏答不得过确认点②）

> 本次适用类别：并发控制 / 子表对账与软删策略 / 状态联动（定时任务/生命周期钩子）/ 统计口径 / 权限与数据范围 / 回显与回传闭环策略 / 全链路时间等格式 / 外部依赖降级（+ 本次扩展：…）。「不适用」也要写明。

| # | 决策 | 说明 |
|---|---|---|
| D1 | … | … |

## 7. 风险与边界

已知风险、预期行为（如无数据源字段恒 0）、平台能力依赖。

---

# implement.md 骨架

> 公共资产清单非空时：**第一个 child 必须是骨架 child**（交付枚举/常量/DDL/包目录/公共子结构），完成前业务 child 不得启动。

| 顺序 | child | 覆盖功能点 | 依赖 | 共享物归属（本 child 创建的公共类/枚举/常量/DDL；复用谁的；他人已建/在途组件锚点） |
|---|---|---|---|---|
| C1 | 骨架 | — | 无 | 创建：…；他人锚点：… |
| C2 | … | F03/F04 | C1 | 复用：C1 枚举/常量 |

## 横切功能点核验矩阵（横切功能点不单独建 child）

> 识别依据：第一段主线串联的「触发角色 / 状态窗口」列——无独立触发角色、代码落点分散在多个接口的功能点在此登记；宿主 child 完成后逐行核验补记「核验结论」列。

| 横切功能点 | 预期联动 | 落点宿主 child | 核验结论 |
|---|---|---|---|
| F08 状态流转 | save 注册 / edit 重注册 / terminate·delete 移除 / 处理器幂等守卫 | C2、C3 | （宿主完成后补记） |

## 验证方式

- 每个 child：编译命令 / 自测口径 / 对照技术级验收标准

## 回滚点

- 按 child 粒度 revert；表结构回滚风险说明
