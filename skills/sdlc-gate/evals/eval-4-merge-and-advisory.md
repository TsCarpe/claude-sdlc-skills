# eval-4：同根因合并 + 备案分层 + soft 依据路由

```json
{
  "skills": ["sdlc-gate"],
  "query": "<需求名> 四个子代理预审都回传了，开始 RECONCILE 和汇总 issues 文件",
  "files": [
    "sdlc/<需求名>/intake/digest-*.md（§7 规则含 FR-08 驳回理由必填；未定义长度上限）",
    "sdlc/<需求名>/design.md（§2.3 字段口径：驳回理由输入框，未提长度限制）",
    "预审回传四条原始发现：架构 A-03「驳回理由无长度上限校验，违反分层校验规范 §4」＋数据 S-07「驳回理由落库字段长度未定义（varchar 无长度）」＋可测性 T-09「列表页脚注文案与设计稿措辞不符（低严重度，改不改两可）」＋架构 A-05「统计口径可能未包含中途撤回的记录（无直接原文支撑，从流程图推导）」"
  ],
  "expected_behavior": [
    "A-03 与 S-07 识别为同根因，合并为一个裁决单元：主行进架构表 A-03，问题列首句写根因（如「驳回理由长度约束在设计与表结构两侧均未定义」），<br> 后写影响范围（命名空间引用，如 功能.F-xx / 用例.TC-xx）",
    "合并主行保留两角色证据并标注来源角色；S-07 不再单独占编号、不留空行",
    "T-09（低严重度且无需动作）进备案区：沿用角色号段编号，不出现在裁决焦点表，不需裁决，放行不因它未裁决被拦截",
    "A-05 标注 soft 依据（无直接原文——依据链一句），进批量预填组而非裁决焦点，行内可见「（无直接原文——依据：…）」标注",
    "速览统计含「原始发现 X 条；同根因合并 Z 组 → 裁决单元 Y 条；备案 n 条」说明，计数与明细行同源",
    "生成后运行 scripts/check_trace.py 校验引用可达与计数同源，exit≠0 时修复重跑（脚本不可得时人工核对并注明降级）"
  ],
  "red_lines": [
    "soft 依据且严重度=高的条目必须仍进裁决焦点——风险优先于不确定性，不得因依据弱降级处理",
    "被并入的同根因条目不得以空行或幽灵编号残留在角色表中"
  ]
}
```
