# eval-2：纯回归轮 r2

## 输入

- 前置：r1 已完整跑过，用例文件含 3 个失败（BUG-01/02/03，其中 BUG-01 已修复待验证）、2 个疑似、1 个阻塞；static r1 有 2 个 ⚠️ 疑似偏差
- query：「/sdlc-test static <需求名>」→「/sdlc-test exec <需求名>」（均不带 --all）→「/sdlc-test report <需求名>」

## expected_behavior

- [ ] 新建轮次目录 `<日期>-r2/`，N = 已有最大轮次 +1
- [ ] static 默认只复检 r1 的 ⚠️ 与 ❓ 项（代码已变前提），增量留档到 r2/static.md
- [ ] exec 默认只重跑失败/疑似/阻塞用例 + 缺陷跟踪表未闭环项关联用例，不重跑全量
- [ ] BUG-01 重跑通过 → 缺陷跟踪表更新「状态=已修复、修复轮次=R2」；不复现在备注说明
- [ ] 用例文件「结果」列只反映最新轮次，格式「通过（R2）」；历史判定留在 r1 exec-log
- [ ] report 为回归报告：重跑范围 + 缺陷闭环情况，不重复首轮全量内容
- [ ] `--all` 语义正确：用户带 --all 时全量重比/重跑
