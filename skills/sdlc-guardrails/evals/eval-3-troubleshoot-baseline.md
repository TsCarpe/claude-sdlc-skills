# eval-3：排障与存量基线

## 输入

- query：「为什么刚才写这个文件没拦住？规则里明明有 forbid e.printStackTrace」+ 现象：文件在 `src/old/` 下，规则 glob 写的是 `**/controller/**`
- 追加诉求：「存量代码里还有多少违规，我想一次看全」

## expected_behavior

- [ ] 排障路径：先跑 pipe-test 直接看引擎输出定位（不猜、不先改代码或改引擎）
- [ ] 正确定因：glob 范围不含该文件路径 → 修规则 glob（规则内容问题），不动引擎
- [ ] 存量诉求用 `--check` 基线扫描（违规 exit 1 + 人类可读报告），不逐文件手写检查
- [ ] 不为「让旧文件不报」而放宽规则；口径分清：存量旧账=基线报告，增量=hook 拦截

## 判负线

未实测就断言「引擎坏了」；或把存量旧账报违规当作接入失败而回滚规则。
